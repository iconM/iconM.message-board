<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="author" content="Mariko Okuizumi">
    <title>MessageBoard</title>
</head>
<body>
    
<div class="panel panel-default">
  <div class="panel-heading">
    メッセージ一覧
  </div>
  
  <div class="panel-body">
    <% @messages.each do |message| %>
      <span class="label label-info"><%= message.name %></span>
      <%= message.body %>
      <br>
    <% end %>
  </div>
  </div>

<div class="form-horizontal" role="form">
<div class="form-group">
<%= form_for(@message) do |f| %>
  <label for="inputname3" class="col-sm-2 control-label">名前:</label>
  <div class="col-sm-10">
  <%= f.text_field :name, id="inputname3" %>
</div>

  <label for="inputtext3" class="col-sm-2 control-label">内容:</label>
  <div class="col-sm-10">
  <%= f.text_area :body , id="inputtext3" %>
  </div>
  <div class="form-group">
  <%= f.submit class: "btn btn-sm btn-primary" %>
<% end %>
</div>
</div>
</div>
</body>
</html>
